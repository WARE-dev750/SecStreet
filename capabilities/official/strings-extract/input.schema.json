{
  "type": "object",
  "required": ["path"],
  "properties": {
    "path": { "type": "string", "minLength": 1 },
    "minLength": { "type": "number", "minimum": 1, "maximum": 255 },
    "contains": { "type": "string", "description": "Optional substring filter; only strings containing this value are returned." },
    "limit": { "type": "number", "minimum": 1, "maximum": 100000 }
  }
}
